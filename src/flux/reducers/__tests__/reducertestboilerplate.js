/*
import { ACTION_TYPE } from "../../actions/types";
import { sampleReducer } from "../sampleReducer";

// this is the default value of of the reducer when action is not passed
test("", () => {});

// return value when action is being fired
test("returns `` when receiving an action type of ``", () => {
  const newState = Reducer();
  expect(newState).toEqual();
});
*/
